[client]
{
    #----------------------------------------------------------
    # Read (and possibly create) mssql password
    #----------------------------------------------------------

    use esmith::util;
    use NethServer::Password;
    my $pw = NethServer::Password::store('mssql');
    $OUT .= "password=$pw";
}